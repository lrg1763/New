import { createContext } from 'react' // Импортируем функцию createContext из библиотеки React

// Создаем контекст для заказов с начальным значением пустого объекта
export const OrdersContext = createContext({})
